import { DisplayStrokesPipe } from './display-strokes.pipe';

describe('DisplayStrokesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayStrokesPipe();
    expect(pipe).toBeTruthy();
  });
});
