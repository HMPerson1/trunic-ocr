import { InfoDialogOpenButtonDirective } from './info-dialog-open-button.directive';

describe('InfoDialogOpenButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoDialogOpenButtonDirective();
    expect(directive).toBeTruthy();
  });
});
